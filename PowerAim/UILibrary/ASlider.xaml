<UserControl x:Class="PowerAim.UILibrary.ASlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PowerAim.UILibrary"
             mc:Ignorable="d"
             d:DesignWidth="282.5"
             Height="74">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title row -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       x:Name="SliderTitle"
                       Padding="2,8,10,4"
                       FontFamily="Segoe UI Variable Text"
                       Text="Mouse Sensitivity"
                       Foreground="{DynamicResource FluentTextPrimary}"
                       TextTrimming="CharacterEllipsis"
                       FontSize="13"
                       VerticalAlignment="Center" />
            <Label Grid.Column="1"
                   x:Name="AdjustNotifier"
                   Padding="10,8,2,4"
                   FontFamily="Segoe UI Variable Small"
                   Content="0.95 Sensitivity"
                   Foreground="{DynamicResource FluentTextSecondary}"
                   VerticalContentAlignment="Center"
                   FontSize="12"
                   HorizontalContentAlignment="Right" />
        </Grid>

        <!-- Slider row: - button | slider | + button -->
        <Grid Grid.Row="1" Height="34" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"
                    x:Name="SubtractOne"
                    Style="{StaticResource FluentIconButton}"
                    Content="&#xE949;"
                    Width="28"
                    Height="28"
                    MinWidth="28"
                    MinHeight="28"
                    Padding="0"
                    FontSize="10"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center" />
            <Slider Grid.Column="1"
                    x:Name="Slider"
                    Margin="10,0,10,0"
                    Height="28"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource FluentAccent}"
                    Background="{DynamicResource FluentSurface3}"
                    IsSnapToTickEnabled="True"
                    Value="2"
                    TickFrequency="0.01" />
            <Button Grid.Column="2"
                    x:Name="AddOne"
                    Style="{StaticResource FluentIconButton}"
                    Content="&#xE948;"
                    Width="28"
                    Height="28"
                    MinWidth="28"
                    MinHeight="28"
                    Padding="0"
                    FontSize="10"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center" />
        </Grid>
    </Grid>
</UserControl>

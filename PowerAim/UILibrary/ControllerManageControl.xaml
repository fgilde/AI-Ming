<UserControl x:Class="PowerAim.UILibrary.ControllerManageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="320" d:DesignWidth="520">
    <StackPanel>
        <!-- Rows are built in code-behind (BuildRow) so the per-row chips/actions stay simple — same
             idiom as HiddenControllersPanel. -->
        <StackPanel x:Name="DevicesPanel" />
        <TextBlock x:Name="StatusText"
                   Margin="2,6,2,0"
                   FontFamily="Segoe UI Variable Small"
                   FontSize="11"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource FluentTextTertiary}" />
    </StackPanel>
</UserControl>

<UserControl x:Class="PowerAim.Visuality.MessageDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             HorizontalAlignment="Center"
             VerticalAlignment="Top"
             MinWidth="380"
             MaxWidth="560">
    <Border Background="{DynamicResource FluentSurface1}"
            BorderBrush="{DynamicResource FluentStroke}"
            BorderThickness="1,0,1,1"
            CornerRadius="0,0,12,12"
            Padding="20,18,20,16">
        <Border.Effect>
            <DropShadowEffect BlurRadius="22" ShadowDepth="6" Opacity="0.28" Direction="270" Color="Black" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header: icon + title -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border x:Name="IconHolder" Grid.Column="0" Width="32" Height="32" CornerRadius="8"
                        Background="{DynamicResource FluentAccent}"
                        Margin="0,0,12,0"
                        VerticalAlignment="Center">
                    <TextBlock x:Name="IconGlyph"
                               Text="&#xE946;"
                               FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets"
                               FontSize="16"
                               Foreground="{DynamicResource FluentAccentForeground}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Border>
                <TextBlock Grid.Column="1"
                           x:Name="TitleText"
                           Foreground="{DynamicResource FluentTextPrimary}"
                           FontFamily="Segoe UI Variable Display"
                           FontWeight="SemiBold"
                           FontSize="16"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap" />
            </Grid>

            <!-- Message -->
            <TextBlock Grid.Row="1"
                       x:Name="MessageText"
                       Foreground="{DynamicResource FluentTextSecondary}"
                       FontFamily="Segoe UI Variable Text"
                       FontSize="13"
                       LineHeight="18"
                       TextWrapping="Wrap"
                       Margin="0,0,0,14" />

            <!-- Buttons -->
            <StackPanel Grid.Row="2"
                        x:Name="ButtonsPanel"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right" />
        </Grid>
    </Border>
</UserControl>
